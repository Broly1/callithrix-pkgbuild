# Maintainer: Erik Dubois <erik.dubois@gmail.com>
pkgname=callithrix-calamares-config
_pkgname=callithrix-calamares-config
_destname1="/etc"
_destnamescript="/usr/bin"
pkgver=25.10
pkgrel=01
pkgdesc="calamares config for callithrix"
arch=('any')
url="https://github.com/Broly1/${_pkgname}"
license=('GPL3')
makedepends=('git')
depends=()
provides=("${pkgname}")
options=(!strip !emptydirs)
source=(${_pkgname}::"git+$url")
sha256sums=('SKIP')
install='readme.install'
package() {
	mkdir -p "${pkgdir}${_destname1}"
	cp -r "${srcdir}/${_pkgname}/${_destname1}"* "${pkgdir}"
	mkdir -p "${pkgdir}${_destnamescript}"
}
